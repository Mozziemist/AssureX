<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/colorAccentLight</item>
        <item name="background">@drawable/gradient_background</item>
        <item name="editTextColor">@color/textLight</item>
        <item name="editTextStyle">@color/typeTextLight</item>
    </style>
    
    <style name="TextAppearance.AppCompat.Widget.ActionBar.Title" parent="@android:style/TextAppearance">
        <item name="android:textColor">?attr/editTextColor</item>
        <item name="android:textSize">30dp</item>
    </style>

    <style name="DarkTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimaryD</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/colorAccentDark</item>
        <item name="background">@drawable/gradient_background_dark</item>
        <item name="editTextColor">@color/textDark</item>
        <item name="editTextStyle">@color/typeTextDark</item>
    </style>


</resources>
